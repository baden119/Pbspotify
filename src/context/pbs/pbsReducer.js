//Has all actions relating to state

import {
  } from '../types';

  export default (state, action) => {
    switch (action.type) {
        // case SEARCH_USERS:
        //     return {
        //     ...state,
        //     users: action.payload,
        //     loading: false
        //     };
        default:
            return state;
    }
};